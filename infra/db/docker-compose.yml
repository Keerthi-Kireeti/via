services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: transitlink
      POSTGRES_USER: transitlink
      POSTGRES_PASSWORD: transitlink
    ports:
      - "5432:5432"
    volumes:
      - transitlink_pg:/var/lib/postgresql/data

volumes:
  transitlink_pg:
