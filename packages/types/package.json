{
  "name": "@transitlink/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'no build required for package types'",
    "lint": "echo 'lint not configured for package types'",
    "typecheck": "echo 'no typecheck for package types'"
  }
}
