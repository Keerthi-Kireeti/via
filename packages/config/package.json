{
  "name": "@transitlink/config",
  "version": "0.1.0",
  "private": true,
  "main": "env.ts",
  "types": "env.ts",
  "scripts": {
    "build": "echo 'no build required for package config'",
    "lint": "echo 'lint not configured for package config'",
    "typecheck": "echo 'no typecheck for package config'"
  }
}
